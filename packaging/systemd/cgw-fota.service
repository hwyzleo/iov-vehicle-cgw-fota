# CGW-FOTA systemd unit (CGW-FOTA-DSN-CR-008 §12.4)
#
# FOTA owns this baseline unit. Platform-specific values (User/Group,
# capability, device allowlist, memory/task/fd limits, enable policy) are
# fixed by the CGW-BUILD Platform Manifest and applied via an authorized
# drop-in; the baseline must not default to root.
#
# /etc/cgw is read-only; /var/lib/cgw/fota is the only writable state path.
# SIGTERM triggers the bounded shutdown (CR-007 §11.6); TimeoutStopSec must
# be >= the application shutdown budget and is CI-checked.
[Unit]
Description=CGW FOTA Inventory Service
After=network.target cgw-diag.service
Wants=cgw-diag.service

[Service]
Type=simple
ExecStart=/usr/bin/cgw-fota
Restart=on-failure
RestartSec=2s
TimeoutStopSec=30s
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/cgw/fota

[Install]
WantedBy=multi-user.target
